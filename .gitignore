# ===== LaTeX 编译中间产物 =====
# 这些文件是“本机编译缓存”，里面含本机绝对路径（如 D:/code/cleaning-tools/...）。
# 提交给别人后，别人机器上的 TeXStudio / LaTeX Workshop 预览、SyncTeX 定位、
# 首次编译都会受这些过期缓存干扰（表现为“图片/PDF 显示不出来”或报错）。
# 因此一律不提交，编译时会自动重新生成。
*.aux
*.log
*.toc
*.out
*.synctex
*.synctex.gz
*.synctex(busy)
*.fls
*.fdb_latexmk
latexmk.out
texput.fls

# latexmk / LaTeX Workshop 的临时目录
.texpadtmp/

# 注意：main.pdf 是给老师/审阅用的成品，仍然提交，故此处不忽略。

# ===== 调试/临时文件（与论文内容无关） =====
# .tmp_dbg/ 是排查编译问题时生成的诊断脚本与输出，build*.txt 是构建日志导出。
# 这些文件曾被误提交进仓库，已于提交 dfcc10e 之后一并移除，故在此忽略。
.tmp_dbg/
build*.txt

# ===== 操作系统垃圾文件 =====
.DS_Store
Thumbs.db
